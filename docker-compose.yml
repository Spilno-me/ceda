version: '3.8'

services:
  ceda:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
    volumes:
      - ./src:/app/src
      - ./test:/app/test
    command: yarn test:watch

  # Future: Add vector DB, embedding service, etc.
  # qdrant:
  #   image: qdrant/qdrant:latest
  #   ports:
  #     - "6333:6333"
